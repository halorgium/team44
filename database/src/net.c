#include <stdio.h>
#include <stdlib.h>
#include "net.h"

int startNetworkServer() {
    /* Start network listener */
    fprintf(stderr, "Start network listener\n");

    while(1){}
    
    sleep(10);
    
    return 1;
}
